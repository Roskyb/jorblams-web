{
	"$schema": "https://json.schemastore.org/claude-code-settings.json",
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Edit|Write|MultiEdit",
				"hooks": [
					{
						"type": "command",
						"command": "node -e \"let d='';process.stdin.on('data',c=>d+=c).on('end',()=>{try{const j=JSON.parse(d);const f=j.tool_input?.file_path||j.tool_response?.filePath||'';if(f)console.log(f)}catch{}})\" | (read -r f; [ -n \"$f\" ] && npx --no-install biome check --write --files-ignore-unknown=true \"$f\") 2>/dev/null || true",
						"statusMessage": "Running biome on edited file..."
					}
				]
			}
		]
	}
}
